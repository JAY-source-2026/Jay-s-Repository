# 업로드된 원본 자료 — 인정서·카탈로그·성적서 등.
# 이 저장소는 공개(public)이므로 원본 파일은 절대 커밋하지 않는다.
# 사이트에는 여기서 추출한 데이터(assets/data/*.json)만 올린다.
company-site/docs/**
!company-site/docs/**/
!company-site/docs/**/.gitkeep

# 개인 작업물 — 홈페이지와 무관
call-video/
